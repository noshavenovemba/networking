--- data sctruct ---
List - ordered and changeable, allows duplicate members.
Tuple - ordered and unchangeable, allows duplicate members.
Set - unordered, unchangeable*, and unindexe, no duplicate members.
Dictionary -  ordered** and changeable, no duplicate members.
mutable: list, dict, set
immutable: int, str, tuple
Insert -> Slicing -> Iterating -> enumerate(x)
print(len(x))
x.reverse()
x.sort()
x.pop(2) // remove third element
cars.append("Honda")
a.insert(2,3.8)

generator // returns iterable set
def csv_reader(file_name):
    for row in open(file_name, "r"):
        yield row

arrays - a single data type elements, lists - any data type elements

----
map() - returns a map object of the results after applying the given function to each item of a given iterable (list, tuple etc.)
# Return double of n
def addition(n):
    return n + n
  
# We double all numbers using map()
numbers = (1, 2, 3, 4)
result = map(addition, numbers)
print(list(result))

--- comprehension lists ---
x = [m**2 for m in range(10) if m>4]

class human:
  name = None
  def get_main(self):
    print("Enter you name: ")
    self.name=input()
  
if __name__ == “main”: execute code only if the file was run directly, and not imported
.self - represent the instance of class, access attributes and methods of clas
___init___ method - called when object is created, initialise object's attributes

f=open("/usr/bin/file.txt","r")
os.remove("xyz.txt")

from random import shuffle
shuffle(["a", "b", "c"])

--- lambda anonymous function, take any number of args ---
x=lambda a: a+10
x(8)
